[package]
name = "coalesce-plugin-sdk"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "SDK for building Coalesce WASM plugins"
repository = "https://github.com/MichaelDanCurtis/Coalesce"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
